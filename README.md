The repo uses a local pin of base.v0.17.3 (for `ocaml-lsp-server`), which is not included in the repo. I should figure out a way to make it build easily on other machines. The reason for that is that base uses the gcc **attribute**((unused)) in `obj_stubs.c` which is not supported by MSVC. There is an open PR for changing it to `(void)`. I'll probably be opening a PR to change it to `CAMLunused` but who knows if and when it would be merged.
